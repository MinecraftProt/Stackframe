package org.minecraftprot.stackframe.diagnostic.registry;

/** Loader-neutral claim capability from the classifier arbitration contract. */
public enum EvidenceCapability {
    IDENTITY,
    SCOPE,
    FACT,
    OWNERSHIP,
    REMEDY
}
