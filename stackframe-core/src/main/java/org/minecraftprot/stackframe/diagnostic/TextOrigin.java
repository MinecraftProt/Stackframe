package org.minecraftprot.stackframe.diagnostic;

/** Provenance category for renderer-safe text, never a retained source object. */
public enum TextOrigin {
    CATALOG,
    EXTERNAL,
    GENERATED
}
