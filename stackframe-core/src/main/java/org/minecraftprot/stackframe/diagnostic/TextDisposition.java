package org.minecraftprot.stackframe.diagnostic;

/** Transformation completed before text crosses the renderer boundary. */
public enum TextDisposition {
    VISIBLE,
    REDACTED,
    GENERALIZED,
    OMITTED
}
