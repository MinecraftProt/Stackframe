package org.minecraftprot.stackframe.diagnostic;

/** Loader-neutral category for an operator-actionable verified location. */
public enum LocationKind {
    FILE,
    CONFIGURATION,
    RESOURCE,
    COMPONENT,
    SOURCE,
    LIFECYCLE,
    OTHER
}
