package org.minecraftprot.stackframe.diagnostic;

/** Operator impact; it does not encode rendering, log level, exit status, or confidence. */
public enum Severity {
    ERROR,
    WARNING,
    NOTE
}
