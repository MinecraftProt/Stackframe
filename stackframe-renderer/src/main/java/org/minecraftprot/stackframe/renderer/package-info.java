/**
 * Deterministic, bounded terminal rendering for completed Stackframe diagnostic
 * documents. Terminal selection is separate from the deterministic renderer;
 * adapters can pass destination-specific capabilities to its selection policy.
 */
package org.minecraftprot.stackframe.renderer;
